<svg width="640" height="640" viewBox="0 0 640 640" xmlns="http://www.w3.org/2000/svg"><rect width="100%" height="100%" fill="white"/><g><polyline data-points="-4,-4 4,-4 4,4 -4,4 -4,-4" data-type="line" data-label="Board outline" points="40,600 600,600 600,40 40,40 40,600" fill="none" stroke="#64748b" stroke-linejoin="round" stroke-width="3.5"/></g><g><polyline data-points="-3,-3 3,-3 3,3 -3,3 -3,-3" data-type="line" data-label="ground top copper boundary" points="110,530 530,530 530,110 110,110 110,530" fill="none" stroke="#e87979" stroke-linejoin="round" stroke-width="3.5"/></g><g><polyline data-points="-3,-3 3,-3 3,3 -3,3 -3,-3" data-type="line" data-label="ground bottom copper boundary" points="110,530 530,530 530,110 110,110 110,530" fill="none" stroke="#7b9bea" stroke-linejoin="round" stroke-width="3.5"/></g><g><rect data-type="rect" data-label="Keepout: keepout" data-x="0" data-y="0" x="285" y="285" width="70" height="70" fill="rgba(168, 85, 247, 0.12)" stroke="#a855f7" stroke-width="0.014285714285714285"/></g><circle data-type="circle" data-label="" data-x="-2" data-y="-2" cx="180" cy="460" r="21" fill="transparent" stroke="#d97706" stroke-width="0.014285714285714285"/><circle data-type="circle" data-label="" data-x="-2" data-y="-2" cx="180" cy="460" r="35" fill="transparent" stroke="#0891b2" stroke-width="0.014285714285714285"/><g id="crosshair" style="display: none"><line id="crosshair-h" y1="0" y2="640" stroke="#666" stroke-width="0.5"/><line id="crosshair-v" x1="0" x2="640" stroke="#666" stroke-width="0.5"/><text id="coordinates" font-family="monospace" font-size="12" fill="#666"></text></g><script><![CDATA[
              document.currentScript.parentElement.addEventListener('mousemove', (e) => {
                const svg = e.currentTarget;
                const rect = svg.getBoundingClientRect();
                const x = e.clientX - rect.left;
                const y = e.clientY - rect.top;
                const crosshair = svg.getElementById('crosshair');
                const h = svg.getElementById('crosshair-h');
                const v = svg.getElementById('crosshair-v');
                const coords = svg.getElementById('coordinates');

                crosshair.style.display = 'block';
                h.setAttribute('x1', '0');
                h.setAttribute('x2', '640');
                h.setAttribute('y1', y);
                h.setAttribute('y2', y);
                v.setAttribute('x1', x);
                v.setAttribute('x2', x);
                v.setAttribute('y1', '0');
                v.setAttribute('y2', '640');

                // Calculate real coordinates using inverse transformation
                const matrix = {"a":70,"c":0,"e":320,"b":0,"d":-70,"f":320};
                // Manually invert and apply the affine transform
                // Since we only use translate and scale, we can directly compute:
                // x' = (x - tx) / sx
                // y' = (y - ty) / sy
                const sx = matrix.a;
                const sy = matrix.d;
                const tx = matrix.e;
                const ty = matrix.f;
                const realPoint = {
                  x: (x - tx) / sx,
                  y: (y - ty) / sy // Flip y back since we used negative scale
                }

                coords.textContent = `(${realPoint.x.toFixed(2)}, ${realPoint.y.toFixed(2)})`;
                coords.setAttribute('x', (x + 5).toString());
                coords.setAttribute('y', (y - 5).toString());
              });
              document.currentScript.parentElement.addEventListener('mouseleave', () => {
                document.currentScript.parentElement.getElementById('crosshair').style.display = 'none';
              });
            ]]></script></svg>
